{
  "name": "hidemyemail-extension",
  "version": "1.3.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit && vite build",
    "zip": "npm run build && rm -f hidemyemail-extension.zip && find dist -type f -exec touch -t 198001010000 {} + && cd dist && find . -type f | LC_ALL=C sort | zip -X -q ../hidemyemail-extension.zip -@"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.6",
    "@types/node": "^26.2.0",
    "happy-dom": "^20.11.2",
    "typescript": "^7.0.2",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
