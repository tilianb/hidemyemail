.gradle/
build/
local.properties
.idea/
*.iml
captures/
.externalNativeBuild/
.cxx/
release.keystore
release.keystore

# Firebase config — per-project, supplied at build/deploy time (enables FCM push).
app/google-services.json
